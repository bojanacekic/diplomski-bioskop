MSSQL_SA_PASSWORD=SmartCinema_2026!Dev
JWT_SECRET_KEY=replace-this-with-a-secret-at-least-32-characters-long

# Optional SMTP configuration (registration works without real values, email sending does not).
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_ENABLE_SSL=true
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=
SMTP_FROM_NAME=Smart Cinema

# LM Studio runs on the host, outside Docker.
LM_STUDIO_URL=http://host.docker.internal:1234
LM_STUDIO_MODEL=qwen3-4b-instruct-2507
